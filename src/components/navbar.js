import React from 'react';

const style = {
    textAlign: 'center',
}

function Navbar(props) {
    return(
        <nav style={style}>
            <h1>Game of Life</h1>
        </nav>
    );
};

export default Navbar;
